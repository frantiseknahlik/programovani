/*
string path = "data.txt";

if (!File.Exists(path))
{
    StreamReader sr = new StreamReader(path);
    
    string line = sr.ReadLine();

    while (line != null)
    {
        Console.WriteLine(line);
        line = sr.ReadLine();
    }
    sr.Close();
}

if (!File.Exists(path))
{
    StreamWriter sw = new StreamWriter(path, true);
    sw.WriteLine("Hello World!");
    sw.WriteLine("třetí řádek");
}
*/

    

string path = "data.txt";

int celkovyVek = 0;
int pocetZaku = 0;

//Podmínka pro kontrolu existence souboru
if ()
{
    //přečíst soubor po řádcích 
    StreamReader sr = new StreamReader(path);
    
    //while
    while (Expression)
    {
        Console.WriteLine(radek);
        //Logika pro zpracování toho řádku -> práce se stringem
    }
    //Výpočet průměru, počet žáků a celkový věk
}
//Jinak -> vypiš chyby, že soubor neexistuje




